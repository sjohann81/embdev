# Drivers

## Defines three things:

- HAL drivers (generic API, thin layer, types + function pointers)
- Native drivers (for peripherals internal to a MCU or SoC)
- External drivers (for external devices, chips, expanders...)
